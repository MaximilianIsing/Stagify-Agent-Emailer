services:
  - type: web
    name: stagify-ai-emailer
    runtime: node
    plan: starter
    buildCommand: npm install --verbose
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health