services:
  - type: web
    name: stagify-ai-emailer
    runtime: node
    plan: starter
    buildCommand: PUPPETEER_SKIP_DOWNLOAD=true npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health